// noinspection JSUnusedGlobalSymbols
export const NODE_ENV = process.env['NODE_ENV']

export const IS_DEV = process.env['DEV']

export const DATE = new Date(Number(process.env['DATE']))

export const GIT_HASH = process.env['GIT_HASH']
export const GIT_STATUS = process.env['GIT_STATUS']
